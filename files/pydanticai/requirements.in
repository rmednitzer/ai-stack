# Direct dependencies for the ai-stack Pydantic AI runtime (all MIT/Apache-2.0).
# This is the SOURCE of truth; the pinned, hashed lock is compiled to
# requirements.txt via:
#
#   uv pip compile files/pydanticai/requirements.in \
#     --generate-hashes --python-version 3.13 \
#     -o files/pydanticai/requirements.txt
#
# (or: make pydanticai-lock). Upper bounds pin the major version.
pydantic-ai-slim[openai,dbos]>=1.106.0,<2
fastapi>=0.115,<1
uvicorn[standard]>=0.49.0,<1
httpx>=0.27,<1
opentelemetry-sdk>=1.27,<2
opentelemetry-exporter-otlp-proto-grpc>=1.27,<2
