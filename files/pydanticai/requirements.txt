# Pydantic AI agentic runtime dependencies (all MIT/Apache-2.0).
#
# Installed at pod startup by the install-deps initContainer
# (pydanticai.buildDeps=true), or baked into a prebuilt image via the adjacent
# Dockerfile (buildDeps=false). Upper bounds pin the major version so a runtime
# install cannot pull a breaking major without a deliberate chart change
# (supply-chain determinism, consistent with ADR-002).
pydantic-ai-slim[openai,dbos]>=1.0,<2
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
httpx>=0.27,<1
opentelemetry-sdk>=1.27,<2
opentelemetry-exporter-otlp-proto-grpc>=1.27,<2
