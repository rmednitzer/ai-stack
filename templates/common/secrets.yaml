{{/*
Secrets for credentials that must not be in values.yaml plaintext.
Generate random values on first install; user overrides via values or
external secret management (ESO / Vault).
*/}}

{{- if .Values.searxng.enabled }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ai-stack.componentName" (dict "Release" .Release "Chart" .Chart "component" "searxng") }}-secret
  labels:
    {{- include "ai-stack.labels" . | nindent 4 }}
  annotations:
    helm.sh/resource-policy: keep
type: Opaque
data:
  {{- if .Values.searxng.secretKey }}
  secret-key: {{ .Values.searxng.secretKey | b64enc | quote }}
  {{- else }}
  secret-key: {{ randAlphaNum 64 | b64enc | quote }}
  {{- end }}
{{- end }}

{{- if .Values.qdrant.enabled }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ai-stack.componentName" (dict "Release" .Release "Chart" .Chart "component" "qdrant") }}-secret
  labels:
    {{- include "ai-stack.labels" . | nindent 4 }}
  annotations:
    helm.sh/resource-policy: keep
type: Opaque
data:
  {{- if .Values.qdrant.apiKey }}
  api-key: {{ .Values.qdrant.apiKey | b64enc | quote }}
  {{- else }}
  api-key: {{ randAlphaNum 48 | b64enc | quote }}
  {{- end }}
{{- end }}

{{- if .Values.openTerminal.enabled }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ai-stack.componentName" (dict "Release" .Release "Chart" .Chart "component" "open-terminal") }}-secret
  labels:
    {{- include "ai-stack.labels" . | nindent 4 }}
  annotations:
    helm.sh/resource-policy: keep
type: Opaque
data:
  {{- if .Values.openTerminal.apiKey }}
  api-key: {{ .Values.openTerminal.apiKey | b64enc | quote }}
  {{- else }}
  api-key: {{ randAlphaNum 48 | b64enc | quote }}
  {{- end }}
{{- end }}

{{- if .Values.mcpo.enabled }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ai-stack.componentName" (dict "Release" .Release "Chart" .Chart "component" "mcpo") }}-secret
  labels:
    {{- include "ai-stack.labels" . | nindent 4 }}
  annotations:
    helm.sh/resource-policy: keep
type: Opaque
data:
  {{- if .Values.mcpo.apiKey }}
  api-key: {{ .Values.mcpo.apiKey | b64enc | quote }}
  {{- else }}
  api-key: {{ randAlphaNum 48 | b64enc | quote }}
  {{- end }}
{{- end }}


{{- if and .Values.externalAPIs.enabled .Values.externalAPIs.providers }}
{{- range $provider := .Values.externalAPIs.providers }}
{{- if not (and $provider.existingSecret $provider.existingSecret.name) }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ai-stack.extapiSecretName" (dict "Release" $.Release "Chart" $.Chart "provider" $provider) }}
  labels:
    {{- include "ai-stack.labels" $ | nindent 4 }}
  annotations:
    helm.sh/resource-policy: keep
type: Opaque
data:
  {{- if $provider.apiKey }}
  api-key: {{ $provider.apiKey | b64enc | quote }}
  {{- else }}
  api-key: {{ "REPLACE_ME" | b64enc | quote }}
  {{- end }}
{{- end }}
{{- end }}
{{- end }}

{{- if .Values.authelia.enabled }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ai-stack.componentName" (dict "Release" .Release "Chart" .Chart "component" "authelia") }}-secret
  labels:
    {{- include "ai-stack.labels" . | nindent 4 }}
  annotations:
    helm.sh/resource-policy: keep
type: Opaque
data:
  {{- if .Values.authelia.oidc.clientSecret }}
  oidc-client-secret: {{ .Values.authelia.oidc.clientSecret | b64enc | quote }}
  {{- else }}
  oidc-client-secret: {{ randAlphaNum 64 | b64enc | quote }}
  {{- end }}
  jwt-secret: {{ randAlphaNum 64 | b64enc | quote }}
  session-secret: {{ randAlphaNum 64 | b64enc | quote }}
  storage-encryption-key: {{ randAlphaNum 64 | b64enc | quote }}
{{- end }}

